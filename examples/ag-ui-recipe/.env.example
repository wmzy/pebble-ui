# Optional: point the recipe at a real AG-UI HTTP backend (LangGraph,
# CrewAI, Agno, Mastra, ...). When unset, the scripted local MockAgent runs
# and no network calls are made.
# VITE_AGUI_URL=http://localhost:8000/agui
