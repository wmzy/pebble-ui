# Required at runtime only — builds and `vite build` never read it.
# Get a key at https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-your-key-here

# Optional: any model your key can access (defaults to gpt-4o-mini)
# OPENAI_MODEL=gpt-4o-mini
