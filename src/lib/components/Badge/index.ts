export {default as Badge} from './Badge';
export type {BadgeProps} from './Badge';
// The skin pieces `Badge` wears, exported for composition (same shape
// and rationale as Button's `buttonVariants`/`buttonSizes`).
export {variants as badgeVariants, sizes as badgeSizes} from './badge-styles';
